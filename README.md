# 韵脚检查工具

一个用于分析和可视化中文歌词或诗歌中押韵模式的Web应用。

## 功能特点

- 🔍 **多类型押韵检测**：支持句尾押韵、句间押韵和句内押韵的自动检测
- 📊 **可视化报告**：生成清晰直观的押韵分析报告
- ⚙️ **可调节参数**：允许调整押韵检测的容差参数，以适应不同的创作风格
- 📱 **响应式设计**：适配不同屏幕尺寸，提供良好的移动端体验
- 🎮 **CRT视觉效果**：包含扫描线、闪烁和辉光效果

## 技术栈

- **前端框架**：纯HTML、CSS、JavaScript
- **模块系统**：ES Modules
- **第三方库**：pinyin-pro（用于中文拼音转换）

## 项目结构

```
rhyme_check/
├── assets/              # 资源文件
│   ├── rhyme-groups.json      # 韵脚分组数据
│   └── rhyme-mappings.json    # 韵脚映射数据
├── components/          # 核心组件
│   ├── rhymeAnalyzer.js       # 韵脚分析模块
│   ├── rhymeConverter.js      # 韵脚转换模块
│   └── rhymeVisualizer.js     # 韵脚可视化模块
├── utils/               # 工具函数
│   └── rhymeMapGenerator.js   # 韵脚映射生成工具
├── app.js               # 应用入口文件
├── index.html           # 页面结构
└── styles.css           # 样式文件
```

## 使用方法

### 启动项目

1. 打开终端，进入项目根目录
2. 运行命令启动本地服务器：
   ```bash
   python -m http.server 8000
   ```
3. 在浏览器中访问：http://localhost:8000

### 使用工具

1. 在左侧文本框中输入你的歌词或诗歌
2. 右侧将自动显示押韵分析结果
3. 可在下方调整检测选项，包括：
   - 检测句间押韵
   - 检测句内押韵
   - 调整各种押韵的容差参数

### 高级选项

- **检测句间押韵**：检测不同行之间的押韵关系
  - 间隔容差：允许押韵词语之间的间隔距离
  - 行差容差：允许押韵词语所在行的行号差异
- **检测句内押韵**：检测同一行内的押韵关系
  - 间隔容差：允许同一行内押韵词语之间的间隔距离

## AI内容说明

本项目由 TRAE SOLO 协助完成，部分算法实现与界面设计由 AI 生成。